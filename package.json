{
  "name": "mixed-rss-reader",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name mixed-rss-reader"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
